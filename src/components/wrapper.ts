export function wrapper() {

  const wrapper = document.createElement("div")

  wrapper.className =
"min-h-screen bg-gradient-to-b from-amber-700 to-rose-900 text-white px-16 sm:px-8"

  return wrapper
}